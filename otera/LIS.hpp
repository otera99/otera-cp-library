#ifndef OTERA_LIS_HPP
#define OTERA_LIS_HPP 1

namespace otera {
    template<typename T>
    long long LIS(std::vector<T> a) {
        int _n = (int)a.size();
        
    }
} // namespace otera

#endif // OTERA_INVERSION_NUMBER_HPP
